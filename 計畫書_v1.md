# 鋼筋會議記錄自動填盤系統 — 初步計畫書 v1

> 版本：v1（初稿）
> 日期：2026/05/12
> 基礎模板：1150504會議記錄_整理版.md

---

## 一、系統目標

每週鋼筋採購會議前，自動從公開來源抓取當週市場盤價，填入既有會議記錄模板，產出 **Word 檔**供承辦人 review，再由員工人工補上內部資料（合約剩餘量、盈餘金額等），最後送交主管。

**核心價值**：把目前承辦人每週手動查 10+ 個盤價來源的工作，壓縮成「按一個按鈕 → 拿到半成品 Word」。

---

## 二、本次討論已確認的需求

| # | 項目 | 確認內容 |
|---|---|---|
| 1 | 輸出格式 | Word 檔，且**版面/章節/表格與現有 markdown 模板完全一致** |
| 2 | 填入範圍 | 表格 + 段落內嵌數值（例如「鋼筋牌價為 SD280:18,900 元/噸」這類描述句也要自動填） |
| 3 | 觸發方式 | 手動（員工開啟系統 → 按執行） |
| 4 | 內部資料 | 無 ERP 串接，由員工在系統內手動填寫 |
| 5 | LLM 供應商 | OpenAI（API key 已就緒） |
| 6 | 歷史資料 | 以本系統第一次執行作為基準，依資料日期累積歷史紀錄 |

---

## 三、技術選型

### 3.1 Web Search 工具方案

OpenAI 本身有提供 web search，但混合策略會更穩：

| 方案 | 適用來源 | 優點 | 缺點 |
|---|---|---|---|
| **OpenAI Responses API 內建 `web_search`** | 變動性新聞、國際廢鋼報導 | 不用額外接 API、語意搜尋能力強 | 每次查詢約 US$0.025–0.030、結果不穩定 |
| **指定網站 scraper（自寫）** | 豐興、中鋼、LME、西本新幹線 | 穩定、免費、可精準定位 | 網站改版要維護 |
| **Tavily / Brave Search API** | 補充來源 | 比 Google 便宜 | 多一個整合點 |

**建議**：
- **固定欄位**（豐興 A1–A4、中鋼 A11–A12、LME A10、西本 A8–A9）→ 指定網站 scraper
- **變動欄位**（日本/美國廢鋼 A5–A6、澳洲鐵礦 A7）→ OpenAI `web_search`
- 兩條路都拿到 → 進 Validator 比對

### 3.2 Word 輸出方案

| 方案 | 評估 |
|---|---|
| python-docx + 預先做好 .docx 模板 | **推薦**。可精確控制中文字體、表格樣式，slot 用書籤或字串替換 |
| pandoc markdown → docx | 快速但中文樣式控制有限 |
| LibreOffice headless | 重，不推薦 |

### 3.3 安全 / 資料庫 / 防呆（已確認的工程基線）

| 項目 | 設計 |
|---|---|
| **使用者登入** | JWT（HS256）+ `HttpOnly`, `Secure`, `SameSite=Lax` cookie。Token 短效（30 分）+ Refresh token 7 天。密碼以 bcrypt hash。 |
| **SQLite 模式** | 啟動時執行 `PRAGMA journal_mode=WAL`、`PRAGMA synchronous=NORMAL`、`PRAGMA foreign_keys=ON`。 |
| **Rate Limit** | 用 `slowapi`（或 Streamlit middleware）對「執行抓取」端點限 **每使用者 10 次/小時**；登入端點限 **5 次/分鐘**。 |
| **OpenAI 成本上限** | 單次執行設 token 預算（例如 50k tokens），超過中止；每日全系統呼叫上限可調。 |
| **CSRF** | 因 SameSite=Lax + POST-only mutation，MVP 不另做 CSRF token；後續評估。 |
| **登入 UI** | Streamlit 自寫登入頁（不用第三方），帳號密碼存 SQLite users 表。 |

### 3.4 模板 Slot 化

把目前的 markdown 模板每個動態數值用 `{{slot}}` 標記。例：

原文：
> 鋼筋牌價為 SD280:18,900 元/噸、SD420W:19,900 元/噸,廢鋼基價為 9,900 元/噸

模板：
> 鋼筋牌價為 SD280:{{fx_sd280}} 元/噸、SD420W:{{fx_sd420w}} 元/噸,廢鋼基價為 {{fx_scrap}} 元/噸

段落內描述句（「上漲 1 元至 363 美元」、「連 3 週平盤」）由 Narrator Agent 根據本週與歷史值動態生成。

---

## 四、欄位分類（沿用先前分析）

### A. 自動抓取（公開盤價）
A1–A4 豐興鋼筋/廢鋼/型鋼 ｜ A5 日本 2H 廢鋼 ｜ A6 美國貨櫃廢鋼 ｜ A7 澳洲鐵礦 ｜ A8 西本新幹線指數 ｜ A9 大陸鐵礦/焦炭/廢鋼/鋼胚 ｜ A10 LME 銅價 ｜ A11 中鋼月盤 ｜ A12 中鋼季盤

### B. 系統自算
B1 人民幣→台幣換算 ｜ B3 漲跌差 ｜ B4 「連 N 週平盤」敘述

### C. 員工手動填寫（無 ERP）
- 採購合約剩餘總量、可使用至年月
- 各項盈餘金額（標餘款、物調款、預估盈虧）
- 已採購/預估尚需採購噸數、工令數
- 會議結論文字、決議內容
- 營造工程物價指數（可選擇半自動，從主計總處抓）

---

## 4.5 程式碼結構原則（擴充性 / 可維護性）

明確要求：**任何「來源」、「LLM」、「儲存」、「輸出格式」都要走抽象介面**，未來換廠商 / 加來源不用改主流程。

```
src/
├── core/                  # 主流程編排（不依賴外部 SDK）
│   ├── orchestrator.py    # 主控
│   ├── slot_schema.py     # Slot 定義（單一事實來源）
│   └── pipeline.py        # 抓取 → 驗證 → 敘述 → 寫檔
├── sources/               # 各資料來源 - 都實作 SourceAdapter 介面
│   ├── base.py            #   abstract: fetch(date) -> dict
│   ├── fengxing.py        #   豐興
│   ├── csc.py             #   中鋼
│   ├── lme.py             #   LME
│   ├── xibenxinganxian.py #   西本
│   └── openai_search.py   #   OpenAI web_search 包裝
├── llm/
│   ├── base.py            # abstract: LLMClient
│   └── openai_client.py   #   實作
├── storage/
│   ├── base.py            # abstract: HistoryStore, UserStore
│   └── sqlite_store.py    #   實作
├── validator/             # 驗證規則（每條規則一個檔）
├── narrator/              # 段落敘述生成（template-based）
├── output/
│   ├── base.py            # abstract: DocumentRenderer
│   └── docx_renderer.py   #   python-docx 實作（未來可加 pdf_renderer）
├── auth/                  # JWT、密碼 hash、Session
├── web/                   # Streamlit 頁面
│   ├── app.py             #   入口
│   ├── pages/             #   分頁
│   └── components/        #   可重用 UI 元件
└── config.py              # 所有閾值、URL、限制集中
```

**原則**：
- 每個 `sources/*.py` 約 100–200 行，獨立可測
- 新增來源 = 新增一個 file + 在 `slot_schema` 註冊 = 零修改主流程
- 所有可變參數（URL、selector、threshold、rate limit 數字）放 `config.py` 或 `.env`，不散落各檔
- 全程加 type hints，用 `mypy --strict` 檢查
- 每個 adapter 都要有對應 unit test（mock HTTP response）

---

## 五、系統架構（v1）

```
┌──────────────────────────────────────────────────────────┐
│                   使用者介面（待定）                       │
│  - 觸發執行 / 檢視結果 / 填內部資料 / 下載 Word            │
└──────────────────────┬───────────────────────────────────┘
                       │
              ┌────────▼─────────┐
              │   Orchestrator    │ ← 載入模板 slot 清單
              └────────┬──────────┘
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
  ┌──────────┐   ┌──────────┐   ┌──────────┐
  │ Scraper  │   │WebSearch │   │ History  │
  │ Agent    │   │ Agent    │   │   DB     │
  │(豐興/中鋼)│   │ (OpenAI) │   │(SQLite)  │
  └────┬─────┘   └────┬─────┘   └────┬─────┘
       │              │              │
       └──────┬───────┴──────────────┘
              ▼
      ┌──────────────┐
      │  Validator    │ ← 多源比對、漲跌幅檢查、單位驗證
      │   Agent       │   失敗 → 退回重抓（最多 N 次）
      └──────┬────────┘
             ▼
      ┌──────────────┐
      │  Narrator     │ ← 生成段落敘述（「上漲 X 元至 Y」）
      │   Agent       │
      └──────┬────────┘
             ▼
      ┌──────────────┐
      │ Word Filler   │ ← python-docx 寫入 .docx 模板
      └──────┬────────┘
             ▼
      ┌──────────────┐
      │   人工 Review │ ← 員工檢視 + 補內部資料
      └──────┬────────┘
             ▼
      ┌──────────────┐
      │ 儲存歷史紀錄  │
      └───────────────┘
```

---

## 六、Stage 規劃

### Stage 0 — 基礎建設（1–2 週）
- [ ] 把 markdown 模板做 slot 化、定義 JSON schema（每個 slot 的名稱、型別、來源、單位）
- [ ] 建立 SQLite 歷史資料庫（schema：date / source / item / value / unit / fetched_at）
- [ ] 製作 Word .docx 範本檔
- [ ] python-docx 寫入 pipeline 打通（空值用 ─ 標示）

### Stage 1 — MVP（2–3 週）
- [ ] 豐興 scraper（A1–A4）
- [ ] 中鋼 scraper（A11、A12）
- [ ] LME 銅價 scraper（A10）
- [ ] 簡易 Validator（只做漲跌幅 ±5% sanity check）
- [ ] Narrator 基礎模板敘述生成
- [ ] 使用者介面 v1（待 §八討論決定）
- [ ] 端到端跑通：點按鈕 → 拿到 Word

### Stage 2 — 擴充來源 + 自我修正（2–3 週）
- [ ] OpenAI web_search 整合（A5–A7）
- [ ] 西本新幹線 scraper（A8–A9）
- [ ] 多源交叉驗證
- [ ] 失敗循環重試機制
- [ ] 信心等級標示（高/中/低 → Word 內以顏色標記）

### Stage 3 — 員工填寫介面 + 流程整合（1–2 週）
- [ ] 內部資料表單（C 類欄位）
- [ ] 整合自動 + 手動 → 單一 Word 輸出
- [ ] 操作手冊 + 上線

**總工期估算**：6–10 週（一人開發）

---

## 七、成本估算（每次執行）

| 項目 | 預估 |
|---|---|
| OpenAI LLM token（GPT-4o-mini for narrator/validator） | US$0.05–0.20 |
| OpenAI web_search（10–20 query） | US$0.25–0.60 |
| 自寫 scraper | $0 |
| **單次合計** | **約 US$0.30–0.80** |
| 每週 1 次，年成本 | **約 US$15–40** |

---

## 八、UI / 部署決策（已確認，2026/05/12 更新）

| 項目 | 決定 |
|---|---|
| UI 風格 | **表單按鈕式**，分 Step：登入 → 設定日期 → 抓取 → 補內部資料 → 預覽 → 下載 Word |
| **前端** | **Next.js 14 (App Router) + TypeScript + Tailwind CSS + shadcn/ui** |
| **後端** | **FastAPI + Python（uv 管理）** |
| 業務核心位置 | 後端 Python：scrapers、python-docx、OpenAI、SQLite |
| 部署方式 | MVP：開發者電腦跑 Next.js (3000) + FastAPI (8000)，員工用 `http://你的IP:3000` 連 |
| API key 儲存 | 後端 `.env`（加進 `.gitignore`） |
| 後續升級路徑 | 區網 → 公司內部 server（前後端可分別容器化） |

**為什麼選這個組合（不選 Streamlit）**：
- 多人併發 / cookie auth / 外觀 / 擴充性都是 Next.js 強項
- python-docx 與 Python 爬蟲生態不放棄
- 前後端職責清楚，未來換前端或加 mobile app 都不影響後端

---

## 九、風險與待解問題

| 風險 | 對策 |
|---|---|
| 來源網站改版 → scraper 壞掉 | 多源備援 + 監控告警 + 改版時人工 fallback |
| 不同媒體同一週數字有差異 | Validator 多源比對，差異大時標「低信心」要人工確認 |
| LLM 段落敘述產生幻覺 | **數值與敘述分離**：敘述模板事先寫好，LLM 只負責填變數，不自由生成 |
| 中文 Word 樣式控制 | 用 .docx 模板而非生成、所有樣式預先綁定 |
| 第一次跑沒有歷史值 → 無法算漲跌 | 第一次手動填上週值作為 baseline |
| 員工誤把舊資料當新資料 | 系統檢查資料日期，過舊要警告 |

---

## 十、下一步

1. 決定使用者介面方向（§八）
2. 我產出 Stage 0 + Stage 1 的詳細技術設計：
   - Slot schema 完整定義
   - Scraper 目標 URL 與 selector
   - Word 模板 .docx 範本
   - Validator 規則細節
3. 評估開發環境（Python 版本、部署位置：本機 / 內網 server / cloud）
